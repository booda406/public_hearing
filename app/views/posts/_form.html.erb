<%= simple_form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  
  </div><div class="field">
    <%= f.label :cover_photo %><br>
    <%= f.file_field :cover_photo %>
  </div>
  
  <div class="field">
    <%= f.input :content, :as => :ckeditor, :input_html => { :ckeditor => { :height => 400, :toolbar => 'mini' } } %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
